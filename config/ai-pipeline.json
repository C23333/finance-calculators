{
  "draft": {
    "cliCommand": "claude",
    "promptMode": "stdin",
    "extraArgs": ["--print", "--output-format", "text"],
    "outputMode": "stdout",
    "timeout": 600000
  },
  "review": {
    "cliCommand": "claude",
    "promptMode": "stdin",
    "extraArgs": ["--print", "--output-format", "text"],
    "outputMode": "stdout",
    "timeout": 600000
  },
  "polish": {
    "cliCommand": "claude",
    "promptMode": "stdin",
    "extraArgs": ["--print", "--output-format", "text"],
    "outputMode": "stdout",
    "timeout": 600000
  },
  "fallback": {
    "cliCommand": "claude",
    "promptMode": "stdin",
    "extraArgs": ["--print", "--output-format", "text"],
    "outputMode": "stdout",
    "timeout": 600000
  }
}
